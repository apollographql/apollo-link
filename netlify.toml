[build]
  base    = "docs/"
  publish = "docs/public/"
  command = "gatsby build --prefix-paths && mkdir -p docs/link && mv public/* docs/link && mv docs public/"
[build.environment]
  NPM_VERSION = "6"
